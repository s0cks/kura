init =
  {
    count: 0,
  }

view msg state = 
  text(state.count)

update msg state =
  match msg
    Increment ->
      {
        count: state.count + 1,
      }
    Decrement ->
      {
        count: state.count - 1,
      }
